version: '3.1'
services:

  mysql:
    image: mariadb:11.4.2
    environment:
      MYSQL_DATABASE: 'wms'
      MYSQL_PASSWORD: 'root'
      MYSQL_ROOT_PASSWORD: 'root'
    container_name: mariadb-server-SQL
    ports:
      - '3306:3306'
    volumes:
      - my-db:/var/lib/mysql

volumes:
  my-db:
